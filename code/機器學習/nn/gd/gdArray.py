import gd as gd

"""
  目標函數：f(x, y, z) = (x-1)^2 + (y-2)^2 + (z-3)^2
  此為凸函數，全域最小值位於點 (1, 2, 3)。
  亦可嘗試其他註解掉的二次函數。
"""
def f(p):
    [x, y, z] = p
    return (x-1)**2+(y-2)**2+(z-3)**2

# 從原點 (0, 0, 0) 開始梯度下降
p = [0.0, 0.0, 0.0]
gd.gradientDescendent(f, p)
